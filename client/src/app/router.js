App.Router.map(function() {
    this.route('articles', { path: '/articles' });
    this.route('photos', { path: '/photos'});
    this.route('login', { path: '/login' });
    this.route('booking', {path: '/booking'});
});

